const { findResource } = require('./findResource')
const { findResources } = require('./findResources')
const { setResourcesInfo } = require('./setResourcesInfo')

module.exports = {
    findResource,
    findResources,
    setResourcesInfo,
}
