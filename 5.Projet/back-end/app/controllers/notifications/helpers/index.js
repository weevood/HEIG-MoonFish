const { findNotification } = require('./findNotification')
const { findNotifications } = require('./findNotifications')
const { setNotificationInfo } = require('./setNotificationInfo')
const { setNotificationsInfo } = require('./setNotificationsInfo')

module.exports = {
    findNotification,
    findNotifications,
    setNotificationInfo,
    setNotificationsInfo
}
